numpy>=1.24
scipy>=1.10
soundfile>=0.12
sounddevice>=0.5      # PortAudio bindings, used by the host-side dev tools in rig/

# pybela — streaming/logging/monitoring of Bela variables from Python.
# Needed from Phase 1 on Bela Gem: Watcher on the board, Streamer here. Unverified
# on the Gem's new IDE (ground-rules 3.1, open question 3) — confirm before relying on it.
# pybela>=0.1
