numpy>=1.24
scipy>=1.10
soundfile>=0.12
sounddevice>=0.5      # CoreAudio I/O for the Mac rig (PortAudio bindings)

# pybela — streaming/logging/monitoring of Bela variables from Python.
# Parked with the Bela: the Mac rig runs the DSP and the harness in one process,
# so there is nothing to stream over a websocket.
# pybela>=0.1
